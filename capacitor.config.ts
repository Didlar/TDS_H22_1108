import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TDS_h22_1108',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
